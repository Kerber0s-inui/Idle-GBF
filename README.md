# Idle GBF

手机优先的本地 PWA 放置 RPG 原型。项目聚焦 Whipper 风格远征周回，以及 GBF 风格的武器盘、召唤、词条和奥义构筑，用于验证移动端 MVP 的核心循环与数值手感。

## 开发命令

```bash
npm install
npm run dev
```

## 验证命令

```bash
npm test
npm run build
npm run e2e
```

## 素材边界

项目不提交、不打包、不再分发 GBF/Cygames 素材。本地开发可以通过 asset key 映射到本机资源；发布模式必须使用原创素材或占位素材。
